{
  "name": "commons",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router": "^5.0.0",
    "react-router-dom": "^5.0.0",
    "webpack": "4.17.1"
  },
  "scripts": {
    "build": "webpack && cp ./build/*.* ../shell/public && cp ./build/*.* ../shell/dll && cp ./build/*.* ../mf1/dll && cp ./build/*.* ../mf2/dll && cp ./build/*.* ../mf3/dll"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "babel": {
    "presets": [
      "react-app"
    ]
  },
  "devDependencies": {
    "webpack-cli": "^3.3.1"
  }
}
